# Keeps only the CI itself from rotting: the Kotlin code and its dependencies are frozen on purpose.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ["*"]
